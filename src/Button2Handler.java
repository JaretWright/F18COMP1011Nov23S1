import javafx.event.Event;
import javafx.event.EventHandler;

public class Button2Handler implements EventHandler {
    @Override
    public void handle(Event event) {
        System.out.println("Assignment 2 is out - pitter patter");
    }
}
